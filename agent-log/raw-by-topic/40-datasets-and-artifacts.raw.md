# 数据集与产物原始记录

<!-- source: session 2026-05-25, filesystem checks -->

本次生成或保留的本地产物：

- `/tmp/Visual-Whole-Body-Control-869104c.tar.gz`：`du -h` 输出约 `470M`。
- `/tmp/Visual-Whole-Body-Control-869104c.tar.list`：`wc -l` 输出 `2431`。
- `/tmp/Visual-Whole-Body-Control.index.lock.stale`：来自失败 checkout 的 0 字节 `.git/index.lock` 备份。
- `Visual-Whole-Body-Control/`：最终项目目录；一次 `du -sh` 检查显示约 `1.9G`，同时因为曾查询不存在的 `.git/index.lock` 打印过一条“无法访问”提示。

没有下载 README 中的 Google Drive 权重。
